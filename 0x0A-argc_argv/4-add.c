#include <stdio.h>

/**
 *
 *
 *
 */

int main(int argc, char *argv[])
{
	int q, w;

	if (argc )
	{
		printf("0\n");
	}
	for (q = 1; q < argv[q]; q++)
	{
		printf("%d\n", )
	if (argc != unsigned int)
	{
		printf("Error\n");
		return (1);
	}
	printf("%d\n", argc);
	return (0);


}
