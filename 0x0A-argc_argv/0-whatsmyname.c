#include <stdio.h>

/**
 * main - print its name
 * @argc: number of character sent
 * @argv: character sent
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
