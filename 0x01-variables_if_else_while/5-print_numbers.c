#include <stdio.h>

/**
 * main -integer of base 10
 *
 * return : always 0 (success)
 */
int main(void)
{
	
	for (int number = 0; number <= 10; number++ ){
		printf("%d", number);
	}	
	putchar('\n');
	
	return (0);
}	
